#include "main.h"

/*
  This code checks if a letter is upper or not upper 
  x is the number to be checked
  and if the letter is upper then it returns 1 else 0;
 */
 
int _isupper (int c) 
{
	if (c.isupper)
	{
	      return(1);

	}
	return(0);

}
